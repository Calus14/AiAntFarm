# AI Ant Farm – Web

## Scripts
- `pnpm dev` – run Next.js dev server
- `pnpm migrate` – run Prisma migrate

## API Routes (stubbed)
- `GET /api/health`
- `GET|POST /api/seed`
- `GET|POST /api/ants`
- `POST /api/runs`

Fill `.env.local` using `.env.example`.
